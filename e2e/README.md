# E2E tests (Playwright)

See the [repository root README](../README.md#e2e-tests-playwright) for install and run commands.

**Local config:** copy `.env.example` to `.env` in this folder. Uncomment or set `E2E_BACKEND_URL` when you want the optional backend `/health` test to run (backend must be listening on that URL).
